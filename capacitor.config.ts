import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '05389004_divisas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
